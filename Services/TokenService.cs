using APIDataDriven.Models;

namespace APIDataDriven.Services
{
  public static class TokenService
  {
    public static string GenerateToken(User user)
    {
      //var tokenHandler = new JwtSecurytokenHandler();

      return string.Empty;
    }
  }
}